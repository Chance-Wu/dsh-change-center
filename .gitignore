# Dependencies
node_modules/

# Build output (published via package.json `files`, not committed)
lib/
dist/

# Test / coverage artifacts
coverage/

# Local DSH data (snapshots, history, policy overrides, stores)
.dsh/

# Editor / OS noise
.DS_Store
*.log

# Local planning notes (not published)
PLAN.md
PLAN-VIBE.md
